このリポジトリは https://github.com/dim0627/hugo_theme_robust をフォークしたもので、
hugo のテーマを Claude で Astro のテーマに変換して、その後手動で調整・修正したもの。

とりあえず動く状態ではあるが、Claude が出力したものを全てチェックしたわけではないので細かいチェックはしていない。

`/tags/` のページは Claude が出力した状態で欠落していたが、欠落したままにしている。
